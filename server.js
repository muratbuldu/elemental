var server = {
	playerId : 0,
	gameId : 0,
	gameAuthToken : "",

	getCurrentCards : function() {
		// TODO get user cards from server
	},

	sendAction : function(act) {
		// TODO send action to the game server
		// return true if the action is accepted by
		// the server.
		// Checking if the action is valid on the 
		// server side a MUST.
	},

	



	// General information repository
	getCardEffect : function(cardId) {

	}
};